version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: academic-api
    ports:
      - "${SERVER_PORT:-9090}:9090"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped